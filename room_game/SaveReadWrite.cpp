//reading and writing saves
#include <fstream>
#include <string>

using namespace std;

class SaveReadWrite
{
  private:
    fstream saveFile;
};

SaveReadWrite::SaveReadWrite(fstream fileIn)
{
  saveFile = 
}
